seed: 42
label_order: [baseline, stress, amusement]
device: auto  # "cuda", "cpu", or "auto"

data:
  dataset_path: data/processed/wesad/hf_dataset
  train_frac: 0.7
  val_frac: 0.15

model:
  hidden_sizes: [128, 64]
  dropout: 0.2

optimizer:
  learning_rate: 0.001
  weight_decay: 0.0001

training:
  epochs: 10
  batch_size: 64
  num_workers: 0
  output_dir: runs/wesad
  save_model: true
  metrics_filename: metrics.json

quantization:
  enable_int8: true
  enable_int4: true
  save_int8_model: true
  save_int4_model: true
  int8_model_filename: model_int8.pt
  int4_model_filename: model_int4.pt
